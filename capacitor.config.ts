import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sharebill.vue.ionic',
  appName: 'sharebill-vue-ionic',
  webDir: 'dist'
};

export default config;
