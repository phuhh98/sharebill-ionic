import type {
  // __version,
  // concat,
  // Config,
  DataFrame,
  // dateRange,
  // Dt,
  // getDummies,
  // LabelEncoder,
  // merge,
  // MinMaxScaler,
  // NDframe,
  // OneHotEncoder,
  // readCSV,
  // readExcel,
  // readJSON,
  // Series,
  // StandardScaler,
  // Str,
  // streamCSV,
  // tensorflow,
  // toCSV,
  // toDateTime,
  // toExcel,
  // toJSON,
  // Utils,
} from "danfojs";

declare global {
  const dfd = {
    DataFrame,
  };
}

export {};
